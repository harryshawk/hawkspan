# Real M2/M4 acceptance ledger - 2026-08-04

## Test boundary

- HawkSpan-D remains idle for HawkSpan D acceptance actions.
- Cross-machine actions use installed HawkSpan D `call-tool.mjs` and
  `peer_call_tool` interfaces.
- M2 and M4 currently execute installed revision
  `02bf503d124bd0dc59d38c48bfc3cbea0a404802`.
- Current installable code revision is
  `f0a608a9ba275c940859981dcd647bbce6df81ed`.
- A final archive for the current revision must be regenerated and verified
  before installation; earlier candidate archives are intentionally stale.
- Harry reduced the sequential-run length from 300 to 150 steps.

## Passed real scenarios

| Scenario | Evidence |
| --- | --- |
| Pause and resume the live queue | `pause-queue` produced queue state `paused`; `resume-queue` produced `running`. The active trainer remained isolated from whole-queue state. |
| Skip and retry a real queued job | `hawkspan-robot-d300-v3` transitioned ready to skipped to ready through HawkSpan D queue control. |
| Simultaneous-job isolation | A second complete target was authorized while another run was active. HawkSpan D refused the second start, identified the active process tree, and did not start another trainer. |
| Ethernet fallback | On installed revision `02bf503`, M2 set `HAWKSPAN_PRIMARY_ENABLED=false`. `link_status` exposed only fallback host `10.44.44.3`, selected it, and read live durable R9 job `job-1785884478151-65a1e8875357` at step 12. Restoring the protected environment selected primary host `10.44.45.3` again. |

## Completed first sequential run

- Target: `hawkspan-robot-150-v5-d-accept-r9`.
- Durable job: `job-1785884478151-65a1e8875357`.
- Readiness fingerprint:
  `9e02928cb11f76359247748d4b3d6e92bfb3e1e3639506dd3f8fe2c280b4ed6f`.
- Staged root:
  `/Users/research/AI/LoRA-Runtime/jobs/hawkspan-robot-150-v5-d-accept-r9--a1f697a8603a`.
- The staged target contains 21 images, 21 mapped ControlNet inputs, 21
  captions, 147 caption variants, and four fixed validation prompts.
- Configuration: 150 steps, checkpoint interval 50, retention 10, fixed seed
  `20260801`.
- The runner, SimpleTuner, accelerate, and trainer form one observed four-process
  tree. The mutable source output path did not exist after startup; only the
  staged runtime output path changed.
- R9 completed 150 of 150 steps in epoch 8 with no traceback, exception, or NaN
  recorded in the trainer log. The runner, SimpleTuner, accelerate, and trainer
  processes all exited after completion.
- Final weights are 178,193,656 bytes with SHA-256
  `4d69a0eb56f4d273dd9343c15833aea019eab0b8bb244ae6d60a11c983720dc6`.
- Complete checkpoints 50, 100, and 150 are preserved. Their LoRA-weight
  SHA-256 values are respectively
  `344bd77082e8a899a42d9d64adb4b07530bfc51227c8ca9c150ebf1cb166aa59`,
  `e5621271530d029793d7ad1dbdeaa6bf1fe5e2eacd7ab10c70afe3ddd474f8ff`,
  and `47d0eaedf6fe54a98ff7bde88afde9da34d0dbd794df863e5bb33b53658b9b6a`.
- Checkpoint training states report steps 50, 100, and 150. Each checkpoint
  contains optimizer, scheduler, random state, SimpleTuner configuration,
  dataset state, checkpoint manifest, and LoRA weights.

## Prepared next run

- Target `hawkspan-robot-150-v6-d-accept-r10` is prepared but deliberately not
  staged while R9 is active.
- R10 preserves 150 steps, checkpoint interval 50, retention 10, seven caption
  variants per image, fixed seed `20260801`, four fixed prompt IDs, and the
  approved ControlNet source.

## Not yet passed on the final installed revision

- Reprove changed revision-bound input refusal.
- Reprove unchanged, explicitly selected checkpoint resume.
- Complete a second sequential 150-step run.
- Install the final current revision on both machines between runs and verify
  responding process paths.
- Perform fixed-prompt evaluation with mapped ControlNet inputs.
- Include required sample renders in the return packet.
- Verify package transfer, intake, hashes, and checkpoint preservation.
- Delete only test-owned transient state and verify datasets, weights,
  checkpoints, packets, and receipts remain.
- Perform the real reboot or unexpected-power-loss recovery scenario.
