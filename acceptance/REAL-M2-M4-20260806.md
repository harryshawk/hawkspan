# Real M2/M4 acceptance ledger - 2026-08-06

## Test boundary

- M2 and M4 execute public HawkSpan v0.3.1 revision
  `8add39bb3c27892a4f8e0f04e518471be0b00a8f` for the uninterrupted R20 run.
- Candidate v0.3.2 remains inactive until R20 completes its automatic
  package-return lifecycle.
- Every completion target uses 21 robot images, 21 matching Canny conditioning
  images, seven caption variants per image, and exactly 100 training steps.
- All cross-machine actions use only the installed HawkSpan tools.

## Current real evidence

| Scenario | State | Evidence |
| --- | --- | --- |
| R20 uninterrupted completion and return | Running | Exact v0.3.1 process PID 79792 reached step 11/100. SimpleTuner logged Canny conditioning, ControlNet creation, and the selected LyCORIS/LoHa adapter. |
| Pause one queued target | Passed | R21 became paused with zero attempts while R20 continued under PID 79792 and the queue remained running. |
| Skip one queued target | Passed | R22 became skipped with zero attempts while R20 remained the sole active trainer. |
| Admit jobs while a trainer is active | Candidate fix passed locally | v0.3.2 allows immutable queue admission while preserving one active trainer. Full 27-test gate passed. |
| Reject malformed scheduler authorization | Candidate fix passed locally | v0.3.2 rejects non-`training`, missing, wrong-state, and target-mismatched durable jobs at admission and launch. |
| Truthful launch telemetry | Candidate fix passed locally | v0.3.2 records a successful launch as running without premature `finished_at` or `exit_code`. |
| Clean immutable runtime staging | Candidate fix passed locally | v0.3.2 hashes and copies only target images, captions, and conditioning images. Generated cache metadata cannot change a revision or enter a new staged job. |

## Required remaining evidence

- R20 reaches step 100, packages automatically, returns to M2, and passes
  identity, size, SHA-256, checkpoint, configuration, caption, log, and
  ControlNet evidence checks.
- v0.3.2 is activated atomically on both machines and both authority audits
  report the exact final candidate revision with no live paths from another
  release.
- R20 completes and returns a verified packet, proving the v0.3.1 lifecycle.
  Because its staged conditioning directory inherited generated metadata from
  older runs, it does not count toward the four clean isolated completions.
- Four independent v0.3.2 100-step runs complete and return verified packets:
  R23, R24, R25, and R26.
- A valid queued job is paused and resumed under v0.3.2.
- A valid queued job is skipped and explicitly retried under v0.3.2.
- Separate active test jobs are stopped early and killed through the guarded
  process-tree control; no package is produced for an incomplete attempt and no
  process survives.
