# Claude Code instructions

Read and follow `AGENTS.md` as the authoritative HawkSpan installation and
operation guide. Do not infer permission to configure, replace, stop, remove,
or inspect another peer-link product.

For the bundled SimpleTuner robot examples, also follow
`examples/simpletuner/hawkspan-robots/README.md`. Validate the bundle first,
copy it to private local workload state, and never train or write outputs in
the public repository.
